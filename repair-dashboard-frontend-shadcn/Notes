what is a CommonJS module, what is an ES module

What is a route in the context of app.get('/api/test', (req, res) => {

Mariadb vs mysql

I noticed something like you can keep a context in it's own file, and then when that context is imported it can be shared. Why?


Non polish remaining work:

Required:
Dashboard
Customers page

Account authentication
Sign in page
Sidebar account widget

Figure out docker deployment
Make database
  Make tables
Make backend API with database connected
Make frontend interface with backend

Nice to add after:
Global Search
Settings page
Help Page?
Generate PDFs of invoices
Actual message sending
Message attachments

Polish:
Add breadcrumbs
Make invoices stats link to relevant invoices
Make tickets grouped columns line up
Make settings get saved
Add light/dark mode, improve github icon
Give messages animations when posted
Deletable messages



import { useState, useEffect } from 'react'
import './App.css'

function App() {
  const [customers, setCustomers] = useState([]);
  const [tickets, setTickets] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    // Fetch both customers and tickets
    Promise.all([
      fetch('http://localhost:3001/api/customers').then(res => res.json()),
      fetch('http://localhost:3001/api/tickets').then(res => res.json())
    ])
    .then(([customersData, ticketsData]) => {
      setCustomers(customersData);
      setTickets(ticketsData);
      setLoading(false);
    })
    .catch(err => {
      setError('Failed to fetch data');
      setLoading(false);
    });
  }, []);

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error: {error}</div>;

  return (
    <div className="App">
      <h1>Repair Dashboard</h1>
      
      <section>
        <h2>Recent Tickets</h2>
        <div className="ticket-list">
          {tickets.map(ticket => (
            <div key={ticket.id} className="ticket-card">
              <div className="ticket-header">
                <h3>Ticket #{ticket.id}</h3>
                <span className={`status ${ticket.status}`}>{ticket.status}</span>
              </div>
              <p><strong>Customer:</strong> {ticket.customer_name}</p>
              <p><strong>Phone:</strong> {ticket.customer_phone}</p>
              <p><strong>Issue:</strong> {ticket.issue_description}</p>
              <p><strong>Created:</strong> {new Date(ticket.created_at).toLocaleDateString()}</p>
            </div>
          ))}
        </div>
      </section>

      <section>
        <h2>Customers</h2>
        <div className="customer-list">
          {customers.map(customer => (
            <div key={customer.id} className="customer-card">
              <h3>{customer.name}</h3>
              <p>Email: {customer.email}</p>
              <p>Phone: {customer.phone}</p>
              <p>Added: {new Date(customer.created_at).toLocaleDateString()}</p>
            </div>
          ))}
        </div>
      </section>
    </div>
  )
}

export default App